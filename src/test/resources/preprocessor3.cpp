#include "test1.h"
#if 0 > 5
#include "pch.h"
#include <pch.h>
template <typename T>
T myMax(T x, T y)
{
    return (x > y) ? x : y;
}
int main(int argc, char *argv[])
{
    int a = 1;
    return 0;
}
#endif
#include <pch.h>