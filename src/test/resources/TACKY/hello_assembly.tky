Program("main")

Function("main", true, [

    //i = Var("i")
    //i = Constant(ConstInt(1))

    // the rules for assembly generation for the return statement are on page 18
    return(Constant(ConstInt(2)))

])