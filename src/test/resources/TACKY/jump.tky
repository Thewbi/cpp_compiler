Program("main")

Function("main", true, [

    Jump(target_label)

    //JumpIfZero(Constant(ConstInt(0)), target_label)

    //JumpIfNotZero(Constant(ConstInt(0)), target_label)

    //tmp.0 = Var("tmp.0")
    //tmp.0 = 0
    //JumpIfZero(tmp.0, target_label)

    //tmp.0 = Var("tmp.0")
    //tmp.0 = 1
    //JumpIfNotZero(tmp.0, target_label)

    printf("This should not print!");
    return(Constant(ConstInt(1)))

Label(target_label)
    return(Constant(ConstInt(0)))

])