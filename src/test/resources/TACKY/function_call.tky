Program("main")

// Function
// Name: "main"
// Global: true
// Parameters: None
// Statements: [ ... ]
// Return: implicitly returns int
Function("main", true, [

    tmp.0 = Var("tmp.0")

    // Function Call
    // provide actual parameters for both arguments
    // provide a parameter for the return value
    funccall(test_function, false, Constant(ConstInt(2)), Constant(ConstInt(3)), tmp.0)

    // check if tmp.0 stores the value 50
    compare = Var("compare")
    compare = tmp.0 == 50
    jumpIfNotZero(compare, end)
    printf("Should contain non-zero (true)")
    return(Constant(ConstInt(1))) // non-zero return value equals error

Label(end)
    return(Constant(ConstInt(0)))

])

// Function
// Name: "test_function"
// Global: true
// Parameters: param_a, param_b
// Statements: [ ... ]
// Return: implicitly returns int
Function("test_function", true, param_a, param_b, [

    tmp.0 = Var("tmp.0")
    tmp.0 = Constant(ConstInt(20)) + Constant(ConstInt(30))

    //return(Constant(ConstInt(0)))
    return(Var("tmp.0"))
    
])