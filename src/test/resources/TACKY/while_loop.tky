//
// int a = 5;
// while (a > 0) {
//    printf("test");
//    a = a - 1;
// }
// return 0;
//

Program("main")

Function("main", true, [

    a = Var("a")
    a = Constant(ConstInt(5))

    tmp.0 = Var("tmp.0")

Label(start_while_loop)

    tmp.0 = a > Constant(ConstInt(0))
    JumpIfZero(tmp.0, end_while_loop)

    printf("test");

    a = a - Constant(ConstInt(1))

    Jump(start_while_loop)

Label(end_while_loop)

    return(Constant(ConstInt(0)))

])